language: generic
notifications:
  email: false
branches:
  - production
  - staging
jobs:
  include:
    - name: Package One
      env: TARGET=packages/one
      language: node_js
      node_js:
        - 14

    - name: Package Two
      env: TARGET=packages/two
      language: node_js
      node_js:
        - 14

script: .travis/job.sh